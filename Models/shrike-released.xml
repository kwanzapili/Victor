<?xml version="1.0"?>

<PropertyList>

    <path>shrike.ac</path>

    <!-- 
	Shrike anti-radar missile - wrapper for missile post-release 
    -->
    <animation>
	<enable-hot type="bool">false</enable-hot>
    </animation>

    <animation>
	<object-name>Shrike</object-name>
    </animation>

    <!-- Don't display the smoke (the submodel will reference the .ac model -->
    <animation>
	<type>select</type>
	<object-name>Smoke</object-name>
	<condition>
	    <equals>
		<property>/sim/variant/name</property>
		<value>wibble</value>
	    </equals>
	</condition>
    </animation>

    <model>
	<name>flaminghot</name>
	<path>Aircraft/victor/Models/Effects/Armament/missile-smoke-trail.xml</path>
	<offsets>
	    <x-m>1</x-m>
	    <y-m>0</y-m>
	    <z-m>0</z-m>
	</offsets>
    </model>

    <animation>
	<type>range</type>
	<min-m>0</min-m>
	<max-m>10000</max-m>
    </animation>

</PropertyList>
